import React from 'react';
import { View } from "react-native";

type MProps = {
    bpm: number,
    tacts: number,
}

export const Metronome = (props: MProps) => {
    return (
        <View>

        </View>
    );
};
